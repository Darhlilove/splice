# {PACKAGE_NAME} SDK Configuration

# Base URL for API requests
# Set this to your API server URL
{PACKAGE_NAME_UPPER}_BASE_URL=https://api.example.com

# API Key for authentication
# IMPORTANT: Keep this secret! Never commit this file with real values.
{PACKAGE_NAME_UPPER}_API_KEY=your-api-key-here

# Optional: Request timeout in milliseconds (default: 30000)
# {PACKAGE_NAME_UPPER}_TIMEOUT=30000

# Optional: Enable debug logging (default: false in production)
# {PACKAGE_NAME_UPPER}_DEBUG=true
